name: Sync Card Catalog

on:
  schedule:
    - cron: '0 6 * * 1' # every Monday 6am UTC
  workflow_dispatch:
    inputs:
      sync_mode:
        description: 'Sync mode'
        required: true
        default: 'incremental'
        type: choice
        options:
          - incremental
          - full
          - temporal
      set_id:
        description: 'Optional single-set scope for temporal validation, e.g. swsh3. Leave blank for all sets. Temporal mode only.'
        required: false
        default: ''
        type: string

jobs:
  sync:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: sync
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm install
      - run: npm run sync
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          SYNC_MODE: ${{ github.event.inputs.sync_mode || 'incremental' }}
          SYNC_SET_ID: ${{ github.event.inputs.set_id || '' }}
