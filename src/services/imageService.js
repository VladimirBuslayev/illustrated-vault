import { fetchFallbackImage, buildLimitlessGuess } from './src/services/imageService.js';
// fetchFallbackImage(cardId)  — fetches from pokemontcg.io, caches in localStorage
// buildLimitlessGuess(card)   — constructs Limitless TCG CDN URL guess (pure, no HTTP)
